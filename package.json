{
  "name": "employee-registration",
  "version": "1.0.0",
  "description": "Employee registration full-stack app",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && nodemon server.js",
    "client": "cd client && npm start"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "firebase": "^10.14.0"
  }
}
